(library
  (name profint)
  (libraries containers-data)
  ; (modules_without_implementation to)
  ; (preprocess (pps ppx_deriving.show))
)

(menhir
  (modules proprs)
  (flags --explain))

(ocamllex prolex)
