(library
  (name profint)
  (public_name profint.lib)
  (libraries containers-data yojson)
  (preprocess (pps ppx_blob ppx_expect ppx_yojson_conv))
  (preprocessor_deps (glob_files_rec systems/**))
)

(menhir
  (modules proprs)
  (flags --explain))

(ocamllex prolex)
