; (rule
;  (with-stdout-to blobs.ml
;   (progn
;    (echo "(* This file is automatically generated *)\n")
;    (echo "let lib_systems_coq_proof_v = {css|")
;    (cat systems/coq/Proof.v)
;    (echo "|css};;\n"))))

(library
  (name profint)
  (public_name profint.lib)
  (libraries base zarith)
  ; (inline_tests)
  (preprocess (pps
    ppx_blob
    ppx_base
    ppx_yojson_conv
  ))
  (preprocessor_deps (glob_files_rec systems/**))
)

(menhir
  (modules proprs)
  (flags --explain))

(ocamllex prolex)
