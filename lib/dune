(library
  (name profint)
  (public_name profint.lib)
  (libraries base containers-data)
  (preprocess (pps ppx_blob ppx_base))
  (preprocessor_deps (glob_files_rec systems/**))
)

(menhir
  (modules proprs)
  (flags --explain))

(ocamllex prolex)
