(library
  (name profint)
  (public_name profint.lib)
  (libraries zarith re)
  (preprocess (pps ppx_blob))
  (preprocessor_deps (glob_files_rec systems/**))
)

(menhir
  (modules proprs)
  (flags --explain))

(ocamllex prolex)
